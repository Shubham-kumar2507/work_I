#!/bin/bash
# Pre-removal script

set -e

# Stop service if running
if systemctl is-active --quiet iot-agent; then
    systemctl stop iot-agent
fi

# Disable service
systemctl disable iot-agent.service || true

exit 0
